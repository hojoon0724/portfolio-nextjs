export default function ReactIcon({ bgColor, padding }) {
  return (
    <div className="stack-icon" style={{ backgroundColor: bgColor, padding: padding }}>
      <img src="/assets/icons/react-svgrepo-com.svg" alt="react icon" />
    </div>
  );
}
